'use strict';

require('assert');
const should = require('should');

describe('$', function () {
  before(function () {
  });

  after(function () {
  });

  it('should export properly', function () {
    should.exist(require('..'));
  });
});
